% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_comparator_score.R
\name{il_comparator_score}
\alias{il_comparator_score}
\title{Batch String Similarity Scores}
\usage{
il_comparator_score(.data, col_1, col_2, con = NULL)
}
\arguments{
\item{.data}{A data frame or character table name. Table names require
\code{con}.}

\item{col_1, col_2}{Column names (unquoted or character).}

\item{con}{A DBI connection from \code{\link[DBI:dbConnect]{DBI::dbConnect()}}. If \code{NULL}, uses R-side computation.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the two input columns and metric columns
\code{jaro_winkler}, \code{jaro}, \code{levenshtein}, \code{jaccard}, and \code{cosine}.
Unsupported SQL-backend metrics are present as \code{NA}. The result has S3
class \code{il_comparator_score}.
}
\description{
Computes string-similarity metrics between two columns of a data frame
or database table. Useful for profiling data quality and choosing
comparison thresholds. Rows where either column is missing are omitted.
}
\details{
With \code{con = NULL}, all metrics are computed in R with
\code{\link[stringdist:stringdist]{stringdist::stringdist()}}. With a \code{\link[duckdb:duckdb]{duckdb::duckdb()}} connection,
computation is pushed to SQL. DuckDB computes \code{jaro_winkler}, \code{jaro},
\code{levenshtein}, and \code{jaccard}; other DBI connections use R-side computation.
}
\examples{
df <- data.frame(
  name_l = c('John', 'Jane', 'Bob'),
  name_r = c('Jon', 'Janet', 'Bobby')
)
il_comparator_score(df, name_l, name_r)
}
