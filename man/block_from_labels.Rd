% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_suggest_blocking.R
\name{block_from_labels}
\alias{block_from_labels}
\title{Derive Blocking Rules from Labeled Pairs}
\usage{
block_from_labels(.data, labels, columns = NULL, con = NULL)
}
\arguments{
\item{.data}{A data frame or character table name.}

\item{labels}{A data frame with \code{unique_id_l}, \code{unique_id_r}, and
\code{is_match}.}

\item{columns}{Character vector of column names to evaluate. \code{NULL}
for all non-ID columns.}

\item{con}{A DBI connection from \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{column}, \code{recall} (fraction of true
matches caught), and \code{n_matches_caught}.
}
\description{
For each column, computes the fraction of true-match pairs that share
the same value (recall). Helps identify which columns make effective
blocking keys.
}
\examples{
con <- DBI::dbConnect(duckdb::duckdb())
block_from_labels(fake_1000, fake_1000_labels, con = con)
DBI::dbDisconnect(con, shutdown = TRUE)
}
