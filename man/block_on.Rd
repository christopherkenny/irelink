% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_block_on.R
\name{block_on}
\alias{block_on}
\title{Create a Training-Time Blocking Rule}
\usage{
block_on(..., .where = NULL, .transform = NULL, .explode = NULL)
}
\arguments{
\item{...}{Unquoted column names. Columns are AND-ed within a single
\code{block_on()} call.}

\item{.where}{An optional raw SQL string for non-equality blocking
conditions (e.g., \code{"levenshtein(l.dob, r.dob) <= 1"}). When
supplied alongside column names, the column equalities and the SQL
condition are AND-ed together.}

\item{.transform}{An optional transform function applied to both
left and right column values before the equality check. See
\link{il_soundex}, \link{il_metaphone}, and \link{il_dmetaphone}.}

\item{.explode}{An optional character vector of array column names to
unnest before blocking. See \code{\link[=il_block_on]{il_block_on()}} for details.}
}
\value{
A blocking-rule object for use in training verbs.
}
\description{
Creates a blocking rule for use inside training verbs such as
\code{\link[=il_estimate_em]{il_estimate_em()}} and \code{\link[=il_estimate_prior]{il_estimate_prior()}}. This is distinct from
\code{\link[=il_block_on]{il_block_on()}}, which adds prediction-time blocking to a specification.
Analogous to \code{\link[dplyr:join_by]{dplyr::join_by()}} — a specification object that describes
how to partition pairs during training.
}
\examples{
block_on(first_name, surname)

# Fuzzy SQL conditions
block_on(first_name, .where = 'levenshtein(l.dob, r.dob) <= 1')

# Phonetic blocking
block_on(first_name, .transform = il_soundex)
}
