% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_roc.R
\name{il_roc}
\alias{il_roc}
\title{Compute ROC Curve Data}
\usage{
il_roc(model, labels)
}
\arguments{
\item{model}{A trained \code{il_model} object.}

\item{labels}{A data frame of labelled pairs with a logical or integer
match indicator.}
}
\value{
A tibble with columns \code{threshold}, \code{fpr}, and \code{tpr}.
}
\description{
Returns a tidy tibble of false-positive rates and true-positive rates
at each match-probability threshold, for plotting an ROC curve.
Requires labelled pairs. Designed for use with \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}.
}
\examples{
\dontrun{
il_roc(model, labels = labelled_pairs) |>
  ggplot2::ggplot(ggplot2::aes(x = fpr, y = tpr)) +
  ggplot2::geom_line() +
  ggplot2::geom_abline(linetype = "dashed") +
  ggplot2::coord_equal()
}
}
