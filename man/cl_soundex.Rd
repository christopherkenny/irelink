% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl_domain.R
\name{cl_soundex}
\alias{cl_soundex}
\title{Soundex Phonetic Comparison}
\usage{
cl_soundex()
}
\value{
A comparison-level object for use in \code{\link[=il_compare]{il_compare()}} or
\code{\link[=cl_levels]{cl_levels()}}.
}
\description{
Creates a comparison level based on the Soundex phonetic algorithm.
Two strings match if their Soundex codes are identical — useful as a
fallback level within \code{\link[=cl_levels]{cl_levels()}} or \code{\link[=cl_name]{cl_name()}} to catch names
that sound similar but are spelled differently (e.g., Smith/Smyth,
Robert/Rupert).
}
\details{
On DuckDB, Soundex runs via a registered SQL MACRO.
On PostgreSQL, it uses the native \code{soundex()} function.
On SQLite, it falls back to an R-side implementation.
}
\examples{
il_spec() |>
  il_compare(first_name, cl_soundex())

il_spec() |>
  il_compare(
    first_name,
    cl_levels(
      cl_null(),
      cl_exact(),
      cl_jaro_winkler(0.9),
      cl_soundex(),
      cl_else()
    )
  )
}
