% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_transform.R
\name{il_transform}
\alias{il_transform}
\title{Compose Multiple Transforms into a Chain}
\usage{
il_transform(...)
}
\arguments{
\item{...}{Two or more functions to compose, in application order.
Each must be a recognised transform (e.g. \code{tolower}, \code{toupper}, \code{trimws},
\link{il_soundex}, \link{il_metaphone}, \link{il_dmetaphone}).}
}
\value{
A function of class \code{il_transform_chain} that applies all
transforms in order. The individual steps are stored in the
\code{"transforms"} attribute.
}
\description{
Creates a single transform function that applies multiple transformations
in sequence. The first function is applied first and the last function
is applied last.
The result is itself a function that can be passed as the \code{transform}
argument to \code{\link[=il_compare]{il_compare()}} or \code{\link[=il_block_on]{il_block_on()}}.
}
\details{
On the SQL side, the transforms are nested inside-out:
\code{il_transform(tolower, trimws)} becomes \code{TRIM(LOWER(col))}.
}
\examples{
# Lower-case then trim whitespace
tf <- il_transform(tolower, trimws)
tf('  Hello  ')

# Use in a specification
spec <- il_spec() |>
  il_compare(name, cl_exact(), transform = il_transform(tolower, trimws))
}
