% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl_domain.R
\name{cl_zip_code}
\alias{cl_zip_code}
\title{ZIP Code Comparison}
\usage{
cl_zip_code(
  term_frequency = FALSE,
  lat_col = NULL,
  long_col = NULL,
  km_thresholds = c(1, 10, 100)
)
}
\arguments{
\item{term_frequency}{Logical. If \code{TRUE}, adjust match weights by
ZIP code frequency at the highest comparison level. Defaults to
\code{FALSE}.}

\item{lat_col, long_col}{Character. Names of latitude and longitude
columns. Both must be supplied together. When provided, geographic
distance levels are appended before \code{cl_else()}.}

\item{km_thresholds}{Numeric vector of distance thresholds in
kilometres, ordered from strictest to most lenient. Only used when
\code{lat_col} and \code{long_col} are supplied. Defaults to \code{c(1, 10, 100)}.}
}
\value{
A comparison-level object for use in \code{\link[=il_compare]{il_compare()}}.
}
\description{
A pre-built domain comparison for US ZIP codes. Provides levels for
exact match, 5-digit prefix match (normalizes ZIP+4 against plain
5-digit codes), and 3-digit Sectional Center Facility (SCF) prefix
match. Accepts both plain 5-digit (\code{'90210'}) and ZIP+4
(\code{'90210-3456'}) formats. Optionally appends geographic distance
fallback levels when latitude and longitude columns are available.
}
\examples{
il_spec() |>
  il_compare(zip, cl_zip_code())

# With geographic fallback (requires lat/lon columns in the data)
il_spec() |>
  il_compare(zip, cl_zip_code(lat_col = 'lat', long_col = 'lon'))
}
