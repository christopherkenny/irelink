% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_cleanup.R
\name{il_cleanup_all}
\alias{il_cleanup_all}
\title{Remove All irelink Temporary Tables from a Database}
\usage{
il_cleanup_all(con)
}
\arguments{
\item{con}{A DBI connection.}
}
\value{
\code{con}, invisibly.
}
\description{
Drops every table or view whose name starts with \verb{__il_} from a DBI
connection. This is intended as an explicit interactive escape hatch after
failed runs or exploratory sessions. Prefer \code{\link[=il_cleanup]{il_cleanup()}} when cleaning up a
specific live model on a shared connection.
}
\examples{
df <- data.frame(
  unique_id = 1:4,
  name = c('Ann', 'Anne', 'Bob', 'Rob')
)
con <- DBI::dbConnect(duckdb::duckdb())
spec <- il_spec() |>
  il_compare(name, cl_jaro_winkler(0.9)) |>
  il_block_on(name)
model <- il_model(df, spec = spec, con = con)

il_cleanup_all(con)
DBI::dbDisconnect(con, shutdown = TRUE)
}
