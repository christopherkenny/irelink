% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_phonetic.R
\name{phonetic}
\alias{phonetic}
\alias{il_soundex}
\alias{il_metaphone}
\alias{il_dmetaphone}
\title{Phonetic Transform Functions}
\usage{
il_soundex(x)

il_metaphone(x)

il_dmetaphone(x)
}
\arguments{
\item{x}{A character vector to encode.}
}
\value{
A character vector of phonetic codes (same length as \code{x}).
}
\description{
Phonetic algorithms for blocking and comparison transforms. These
functions compute phonetic encodings that group similar-sounding
names together. They are useful for blocking rules that tolerate
spelling variation.
}
\details{
When used as a \code{transform} argument in \code{\link[=il_block_on]{il_block_on()}}, \code{\link[=block_on]{block_on()}},
or \code{\link[=il_compare]{il_compare()}}, the computation is pushed into SQL so data is
never materialized into R.
\subsection{SQL availability}{\tabular{llll}{
   Function \tab DuckDB \tab PostgreSQL \tab SQLite \cr
   \code{il_soundex} \tab ✓ (macro) \tab ✓ (native) \tab comparisons only (R-side) \cr
   \code{il_metaphone} \tab ✗ \tab ✓ (native) \tab ✗ \cr
   \code{il_dmetaphone} \tab ✗ \tab ✓ (native) \tab ✗ \cr
}


SQLite does not expose a way to register scalar R functions as SQL UDFs,
so phonetic transforms cannot be used in \strong{blocking rules} on SQLite.
They continue to work in \strong{comparisons} on SQLite via the R-side gamma
computation path.
}
}
\examples{
il_soundex(c('Smith', 'Smyth'))
il_soundex(c('Robert', 'Rupert'))
}
