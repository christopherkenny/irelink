% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_suggest_blocking.R
\name{il_suggest_blocking}
\alias{il_suggest_blocking}
\title{Suggest Blocking Rules}
\usage{
il_suggest_blocking(
  .data,
  columns = NULL,
  con = NULL,
  link_type = c("dedupe", "link"),
  max_depth = 1L
)
}
\arguments{
\item{.data}{A data frame, dbplyr \code{tbl_lazy}, or character table name.}

\item{columns}{Character vector of column names to evaluate. When
\code{NULL} (the default), all non-ID columns are tried.}

\item{con}{A DBI connection object. Optional when \code{.data} is already
registered in the database.}

\item{link_type}{One of \code{"dedupe"} (default) or \code{"link"}.}

\item{max_depth}{Maximum number of columns to combine in a single
blocking rule. Defaults to \code{1} (single-column rules only).
Set to \code{2} to also evaluate two-column combinations.}
}
\value{
A tibble with columns \code{rule}, \code{n_distinct}, \code{coverage},
\code{n_pairs}, \code{pct_of_cartesian}, and \code{score}, sorted by \code{score}
descending. Higher scores indicate better blocking rules.
}
\description{
Enumerates single-column blocking rules and ranks them by a heuristic
that balances pair reduction against field coverage. Useful for choosing
initial blocking rules before training.
}
\examples{
df <- data.frame(
  unique_id = 1:20,
  first_name = c(
    'John', 'Jon', 'Jane', 'Jane', 'Bob',
    'Bobby', 'Alice', 'Alicia', 'Tom', 'Thomas',
    'John', 'Jon', 'Jane', 'Janet', 'Bob',
    'Robert', 'Alice', 'Alison', 'Tom', 'Tomas'
  ),
  surname = c(
    'Smith', 'Smith', 'Doe', 'Doe', 'Jones',
    'Jones', 'Brown', 'Brown', 'White', 'White',
    'Smith', 'Smyth', 'Doe', 'Doe', 'Jones',
    'Jones', 'Brown', 'Browne', 'White', 'White'
  )
)
con <- DBI::dbConnect(duckdb::duckdb())
il_suggest_blocking(df, con = con)
DBI::dbDisconnect(con, shutdown = TRUE)
}
