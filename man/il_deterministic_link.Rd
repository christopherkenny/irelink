% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_deterministic_link.R
\name{il_deterministic_link}
\alias{il_deterministic_link}
\title{Deterministic Record Linkage}
\usage{
il_deterministic_link(
  .data,
  ...,
  spec,
  con,
  link_type = c("dedupe", "link", "link_and_dedupe")
)
}
\arguments{
\item{.data}{A data frame or tibble (first or only dataset).}

\item{...}{Additional data frames for multi-table linkage.}

\item{spec}{An \code{il_spec} object with blocking rules defined via
\code{\link[=il_block_on]{il_block_on()}}.}

\item{con}{A DBI connection object.}

\item{link_type}{One of \code{"dedupe"} (default), \code{"link"}, or
\code{"link_and_dedupe"}.}
}
\value{
A tibble of exact-match record pairs.
}
\description{
Finds exact-match record pairs using the blocking rules in the
specification, without requiring probabilistic training. This is a
common first step before probabilistic linkage — pairs that match on
all blocking columns are returned directly.
}
\examples{
\dontrun{
con <- DBI::dbConnect(duckdb::duckdb())
spec <- il_spec() |>
  il_block_on(first_name, surname, dob)

exact_matches <- il_deterministic_link(
  voters, spec = spec, con = con
)
}
}
