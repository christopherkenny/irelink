% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_estimate_u.R
\name{il_estimate_u}
\alias{il_estimate_u}
\title{Estimate Non-Match (u) Parameters}
\usage{
il_estimate_u(model, max_pairs = 1e+06)
}
\arguments{
\item{model}{An \code{il_model} object (piped in).}

\item{max_pairs}{Maximum number of random pairs to sample. Defaults to
\code{1e6}.}
}
\value{
An updated \code{il_model} with estimated u parameters.
}
\description{
Estimates the u probabilities (the probability of observing each
comparison level given that the records do \strong{not} match) by randomly
sampling record pairs. Most random pairs are non-matches, so the
observed level frequencies approximate the u distribution.
}
\examples{
\dontrun{
model <- il_model(voters, spec = spec, con = con) |>
  il_estimate_u(max_pairs = 1e6)
}
}
