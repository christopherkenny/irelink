% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_score_missing_edges.R
\name{il_score_missing_edges}
\alias{il_score_missing_edges}
\title{Score Missing Edges Within Clusters}
\usage{
il_score_missing_edges(model, pairs, clusters, threshold = 0)
}
\arguments{
\item{model}{A trained \code{il_model} object.}

\item{pairs}{An \code{il_compared} tibble from \code{\link[=predict.il_model]{predict.il_model()}}.}

\item{clusters}{A tibble from \code{\link[=il_cluster]{il_cluster()}} with columns \code{unique_id}
and \code{cluster_id}.}

\item{threshold}{Numeric match-probability threshold for returned
pairs. Defaults to \code{0}.}
}
\value{
An \code{il_compared} tibble of newly scored pairs (those not
already in \code{pairs}).
}
\description{
Identifies pairs of records within the same cluster that were not
already scored during prediction (e.g. because they were in different
blocking groups), and scores them using the model. This can reveal
low-confidence links that bridge otherwise separate sub-clusters.
}
\examples{
\dontrun{
pairs <- predict(model, threshold = 0.5)
clusters <- il_cluster(pairs)
missing <- il_score_missing_edges(model, pairs, clusters)
}
}
