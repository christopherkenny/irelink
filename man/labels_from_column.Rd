% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-evaluation.R
\name{labels_from_column}
\alias{labels_from_column}
\title{Derive Pairwise Labels from a Ground-Truth Column}
\usage{
labels_from_column(model, labels_col, threshold = 0)
}
\arguments{
\item{model}{A trained \code{il_model} object.}

\item{labels_col}{A string naming the column in the original data that
contains the ground-truth cluster or entity identifier.}

\item{threshold}{Match-probability threshold for selecting predicted
pairs. Defaults to \code{0} to include all candidate pairs.}
}
\value{
A data frame with columns \code{unique_id_l}, \code{unique_id_r}, and
\code{is_match} (integer 0/1).
}
\description{
Given a model and a column name containing cluster or entity IDs,
generates pairwise labels for all predicted pairs. Two records
sharing the same value in \code{labels_col} are labelled as matches.
}
\details{
This is a convenience wrapper: instead of manually building a labels
data frame with \code{unique_id_l}, \code{unique_id_r}, and \code{is_match}, you
supply the column name and let irelink derive everything.
}
\examples{
\dontrun{
labels <- labels_from_column(model, 'cluster')
il_accuracy(model, labels)
}
}
