% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_column_transforms.R
\name{il_substr}
\alias{il_substr}
\title{Extract a Substring Column Transform}
\usage{
il_substr(start, length)
}
\arguments{
\item{start}{Integer start position (1-indexed).}

\item{length}{Integer number of characters to extract.}
}
\value{
An \code{il_column_transform} closure.
}
\description{
Returns a transform that extracts a fixed-width substring from a string
column. The result can be passed as the \code{transform} argument to
\code{\link[=il_compare]{il_compare()}} or \code{\link[=il_block_on]{il_block_on()}}, and composed with other transforms
via \code{\link[=il_transform]{il_transform()}}. On DuckDB, the computation is
pushed into SQL.
}
\examples{
tf <- il_substr(1, 3)
tf(c('Johnson', 'Smith', 'Lee'))

# Use for blocking on the first 3 characters of a name
spec <- il_spec() |>
  il_block_on(last_name, .transform = il_substr(1, 3))
}
