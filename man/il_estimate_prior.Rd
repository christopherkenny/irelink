% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_estimate_prior.R
\name{il_estimate_prior}
\alias{il_estimate_prior}
\title{Estimate the Prior Match Probability}
\usage{
il_estimate_prior(model, ..., recall = 0.7)
}
\arguments{
\item{model}{An \code{il_model} object (piped in).}

\item{...}{Blocking rules created by \code{\link[=block_on]{block_on()}} that define
deterministic matching criteria.}

\item{recall}{A numeric value between 0 and 1 representing the assumed
recall of the deterministic rules. Defaults to \code{0.7}.}
}
\value{
An updated \code{il_model} with the estimated prior.
}
\description{
Estimates the probability that two randomly selected records from the
dataset are a match, using deterministic rules and a recall assumption.
This prior anchors the Fellegi-Sunter model. Analogous to fitting a
base rate before more detailed parameter estimation.
}
\examples{
\dontrun{
model <- il_model(voters, spec = spec, con = con) |>
  il_estimate_prior(block_on(first_name, surname, dob), recall = 0.7)
}
}
