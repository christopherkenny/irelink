% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cl_array_intersect.R
\name{cl_array_min_distance}
\alias{cl_array_min_distance}
\title{Pairwise Array Minimum Distance Comparison}
\usage{
cl_array_min_distance(fn = c("jaro_winkler", "levenshtein"), ...)
}
\arguments{
\item{fn}{Distance function: \code{'jaro_winkler'} (default) or
\code{'levenshtein'}. For \code{'jaro_winkler'}, thresholds are similarity
scores (0–1, descending, strictest first). For \code{'levenshtein'},
thresholds are edit distances (non-negative integers, ascending,
strictest first).}

\item{...}{Numeric thresholds, from strictest to most lenient.}
}
\value{
A comparison-level object for use in \code{\link[=il_compare]{il_compare()}}.
}
\description{
Creates comparison levels based on the best-matching pair of values
across two array columns. For each record pair, every element of the
left array is compared against every element of the right array. The
best score (maximum similarity for \code{'jaro_winkler'}, minimum distance
for \code{'levenshtein'}) is then tested against each threshold.
}
\details{
On DuckDB the pairwise comparison runs in SQL via an UNNEST cross-join
scalar subquery. On SQLite it falls back to an R-side nested apply.
}
\examples{
# Jaro-Winkler: best pairwise similarity >= 0.9 or >= 0.7
il_spec() |>
  il_compare(aliases, cl_array_min_distance('jaro_winkler', 0.9, 0.7))

# Levenshtein: best pairwise edit distance <= 1 or <= 2
il_spec() |>
  il_compare(aliases, cl_array_min_distance('levenshtein', 1, 2))
}
