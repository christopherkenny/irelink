% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_column_transforms.R
\name{il_nullif}
\alias{il_nullif}
\title{Replace a Value with NA Column Transform}
\usage{
il_nullif(value)
}
\arguments{
\item{value}{The value to treat as missing.}
}
\value{
An \code{il_column_transform} closure.
}
\description{
Returns a transform that replaces a specific value with \code{NA}. Commonly
used to convert empty strings to \code{NA} before comparison so that
missing-data levels are triggered correctly. The result can be passed
as the \code{transform} argument to \code{\link[=il_compare]{il_compare()}} or \code{\link[=il_block_on]{il_block_on()}}, and
composed with other transforms via \code{\link[=il_transform]{il_transform()}}. On DuckDB and
PostgreSQL, maps to SQL \code{NULLIF}.
}
\examples{
tf <- il_nullif('')
tf(c('New York', '', 'Chicago'))

# Use before comparison to treat blank city as missing
spec <- il_spec() |>
  il_compare(city, cl_exact(), transform = il_nullif(''))
}
