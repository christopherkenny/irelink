% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_waterfall.R
\name{il_waterfall}
\alias{il_waterfall}
\title{Extract Waterfall Data for a Single Pair}
\usage{
il_waterfall(pairs, which = 1L)
}
\arguments{
\item{pairs}{An \code{il_compared} tibble from \code{\link[=predict.il_model]{predict.il_model()}}.}

\item{which}{An integer index identifying which row (pair) to
decompose. Defaults to \code{1L}.}
}
\value{
A tibble with columns \code{step}, \code{order}, \code{contribution}, and
\code{direction} (positive, negative, prior, or final).
}
\description{
Returns a tidy tibble showing how each comparison contributed to the
total match weight for a specific record pair. Designed for use with
\code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}} and \code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}.
}
\examples{
\dontrun{
il_waterfall(pairs, which = 1) |>
  ggplot2::ggplot(ggplot2::aes(
    x = reorder(step, order), y = contribution, fill = direction
  )) +
  ggplot2::geom_col() +
  ggplot2::coord_flip()
}
}
