% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_cluster_confusion_matrix.R
\name{il_cluster_confusion_matrix}
\alias{il_cluster_confusion_matrix}
\title{Cluster-Level Confusion Matrix for Deduplication}
\usage{
il_cluster_confusion_matrix(
  model,
  labels_col,
  threshold = 0.85,
  method = c("connected", "best_link"),
  ties_method = c("lowest_id", "drop"),
  source_dataset = NULL
)
}
\arguments{
\item{model}{A trained \code{il_model} object for a deduplication task.}

\item{labels_col}{String naming the ground-truth cluster/entity column
in the model's source data.}

\item{threshold}{Match-probability threshold passed to \code{\link[=predict]{predict()}}.
Defaults to \code{0.85}.}

\item{method}{Clustering method passed to \code{\link[=il_cluster]{il_cluster()}}.}

\item{ties_method}{Tie handling for \code{method = "best_link"}, passed to
\code{\link[=il_cluster]{il_cluster()}}.}

\item{source_dataset}{Optional source-dataset mapping passed to
\code{\link[=il_cluster]{il_cluster()}}. If supplied, it must cover every \code{unique_id} in the
predicted pairs, and duplicate \code{unique_id} mappings are not allowed.}
}
\value{
A one-row tibble with columns \code{threshold}, \code{tp}, \code{fp}, \code{fn},
\code{tn}, \code{precision}, \code{recall}, and \code{f1}.
}
\description{
Computes a record-level confusion matrix after clustering predicted
matches into entities. A record is treated as "duplicated" if it is not
the first record in its predicted cluster, and likewise for the
ground-truth \code{labels_col}.
}
\details{
For DuckDB and PostgreSQL backends, pair scoring and clustering are
pushed into SQL where possible. The final summary still returns a
one-row tibble in R.
}
\examples{
df <- data.frame(
  unique_id = 1:5,
  first_name = c('John', 'John', 'Mary', 'Bob', 'Bob'),
  surname = c('Smith', 'Smith', 'Jones', 'Brown', 'Brown'),
  cluster = c(1, 1, 2, 3, 4)
)
con <- DBI::dbConnect(duckdb::duckdb())
spec <- il_spec() |>
  il_compare(first_name, cl_exact()) |>
  il_compare(surname, cl_exact()) |>
  il_block_on(surname)
model <- il_model(df, spec = spec, con = con)
model <- il_estimate_u(model)
model <- il_estimate_em(model, block_on(surname))

il_cluster_confusion_matrix(model, labels_col = 'cluster', threshold = 0.85)
DBI::dbDisconnect(con, shutdown = TRUE)
}
