% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_column_transforms.R
\name{il_try_parse_date}
\alias{il_try_parse_date}
\title{Try-Parse Date Column Transform}
\usage{
il_try_parse_date(format = "\%Y-\%m-\%d")
}
\arguments{
\item{format}{A \code{strptime}-style format string. Defaults to \code{"\%Y-\%m-\%d"}.}
}
\value{
An \code{il_column_transform} closure.
}
\description{
Returns a transform that attempts to parse a string column as a date.
Unlike \code{as.Date()}, failures return \code{NA}/\code{NULL} rather than raising an
error. On DuckDB this uses \code{try_strptime()}.
The result can be passed as the \code{transform} argument to \code{\link[=il_compare]{il_compare()}}
or \code{\link[=il_block_on]{il_block_on()}}, and composed with other transforms via
\code{\link[=il_transform]{il_transform()}}.
}
\examples{
tf <- il_try_parse_date()
tf(c('2020-01-15', 'not-a-date', '1985-06-30'))

# Non-ISO format
tf2 <- il_try_parse_date('\%m/\%d/\%Y')
tf2(c('01/15/2020', 'bad'))
}
