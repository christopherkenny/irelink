% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_column_transforms.R
\name{il_regex_extract}
\alias{il_regex_extract}
\title{Regex Extraction Column Transform}
\usage{
il_regex_extract(pattern, group = 0L)
}
\arguments{
\item{pattern}{A regular expression.}

\item{group}{Integer capture group to extract. Use \code{0} for the whole
match, or a positive integer for a numbered capture group.}
}
\value{
An \code{il_column_transform} closure.
}
\description{
Returns a transform that extracts a regex match from a string column.
Returns \code{NA} when no match is found. The result can be passed as the
\code{transform} argument to \code{\link[=il_compare]{il_compare()}} or \code{\link[=il_block_on]{il_block_on()}}, and
composed with other transforms via \code{\link[=il_transform]{il_transform()}}. On DuckDB and
PostgreSQL, the computation is pushed into SQL.
}
\examples{
# Extract a 5-digit ZIP code from a freeform address string
tf <- il_regex_extract('\\\\d{5}')
tf(c('Apt 4, 90210', '10001-1234', 'no zip'))
}
