% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/il_model.R
\name{il_attach}
\alias{il_attach}
\title{Attach a Saved Model to Fresh Data}
\usage{
il_attach(model, .data, ..., con = NULL, link_type = NULL)
}
\arguments{
\item{model}{An \code{il_model} object, typically from \code{\link[=il_load]{il_load()}}.}

\item{.data}{A data frame, tibble, dbplyr \code{tbl_lazy}, or character
table name. The first (or only) input dataset.}

\item{...}{Additional datasets for multi-table linkage.}

\item{con}{A DBI connection object. Optional when \code{.data} is a
\code{tbl_lazy} — the connection is extracted from the table reference.}

\item{link_type}{Optionally override the model's link type. If \code{NULL}
(default), uses the link type stored in the model.}
}
\value{
The model, now connected to \code{con} with data uploaded, ready
for \code{\link[=predict]{predict()}}, \code{\link[=il_find_matches]{il_find_matches()}}, or further training.
}
\description{
Takes a loaded (or existing) \code{il_model} and binds it to new data and a
fresh database connection, producing a model ready for \code{\link[=predict]{predict()}} or
further training. Accepts in-memory data frames, dbplyr lazy table
references, or character table names.
}
\details{
This is the key function for the production workflow:
train once with \code{\link[=il_model]{il_model()}} → save with \code{\link[=il_save]{il_save()}} → later, load
with \code{\link[=il_load]{il_load()}} and attach to new data with \code{il_attach()}.

The loaded model's trained parameters (m, u, prior) are preserved.
You can immediately call \code{\link[=predict]{predict()}} on the attached model, or
continue training with \code{\link[=il_estimate_em]{il_estimate_em()}} using the existing
parameters as a warm start.
}
\examples{
\dontrun{
# Production workflow: load a pre-trained model, attach to new data
loaded <- il_load('trained_model.json')
con <- DBI::dbConnect(duckdb::duckdb())
model <- il_attach(loaded, new_data, con = con)
pairs <- predict(model)
DBI::dbDisconnect(con, shutdown = TRUE)
}
}
